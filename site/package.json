{
  "name": "@phaseshiftdata/prometheus-exporters-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000",
    "build": "vite build",
    "test:install": "npx playwright install --with-deps chromium",
    "test": "npx playwright test"
  },
  "dependencies": {
    "@asymmetric-effort/specifyjs": "latest"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "@playwright/test": "^1.59.0"
  }
}
